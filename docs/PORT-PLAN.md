# Port DAMS off fcrepo/fin onto CaskFS/project-anduin/argonath

> Living document — keep this updated as phases complete, decisions change, or assumptions turn out to be wrong. Started 2026-09-01. Branch: `argo-dcf`.

## Context

`digital.ucdavis.edu` currently runs on a three-tier stack: `fcrepo` (Fedora Commons LDP repository) → `fin` (a large, separate microservice application layer that wraps fcrepo — gateway/proxy, admin-ui, dbsync-to-Elasticsearch, GCS binary sync, OIDC, etc., in the sibling `fin` repo) → the `dams` client+server (this repo, npm package `ucd-lib-client`). We are retiring fcrepo and fin entirely in favor of **CaskFS** (content-addressed, SHA-256, REST API, built-in RDF graph) running under **project-anduin** (shared Postgres/Dagster/Superset/auth-gateway platform), with **argonath** (Dagster-orchestrated ingestion/AI-metadata/indexing pipeline) on top of it.

The guiding constraint: **touch the client as little as possible in this first pass**. The client keeps calling `/fcrepo/rest/...` URLs; `services/client/lib/fcrepo-middeware.js` (already stubbed, unwired) becomes a translation shim that resolves those calls against CaskFS instead of real fcrepo. Per explicit instruction, **the shim only needs to cover the subset of fcrepo/fin behavior the dams client and server actually exercise** — not fcrepo's full LDP API surface. A commit already on this branch (`72d52ac "start of port to cask"`) started this: it renamed `services/fin` → `services/client` and left three unimplemented branches in the shim (`svc:iiif`, `svc:gcs`, plain resource stream), but also left the repo in a broken state (a stale Dockerfile/`.cork-build`, and an incomplete copy of a model-mounting pattern — see Phase 0).

Two other things this port is explicitly scoped to also do:
- **Client edits** (today: an in-client edit UI — collection hero image, watercolor accent theme, highlighted items, per-item viewer-type override, homepage featured-collections widget, all gated by Keycloak `admin`/`ui-admin` roles) currently writes by having the **browser PUT JSON-LD directly to `/fcrepo/rest/application/ucd-lib-client/...`**, bypassing the server entirely, with Postgres (`dams_edits.edit`) acting only as a read-cache populated indirectly via fin's fcrepo-event-driven `dbsync`. This needs to become a real server-side JSON API, with Postgres as the primary store, dropping JSON-LD/RDF for this data.
- **Branding**: rename from "DAMS" to a "digital collections" / `dcf` identity (branch is already `argo-dcf`) — internal tokens only this pass (npm packages, Postgres schema, custom-element/CSS prefixes, GCS bucket names, user-facing text). GitHub repo names (`dams`, `dams-deployment`) stay as-is for now.
- **Start a real test suite** — neither `dams` nor `fin` has one today. The `caskfs` repo has a working Mocha convention to mirror.

### Decisions already confirmed
- **Namespace strategy**: the frontend keeps its **own namespace(s)**, calling `cask`/`anduin-gateway` in `argonath-prod` **cross-namespace** via k8s DNS. This requires solving "how does frontend dev/test reach a non-prod cask," since `argonath-deployment` currently has **no dev/sandbox namespace at all** — only `prod` + local compose.
- **Auth realm**: adopt Keycloak realm `internal` (argonath's realm) going forward, migrating/re-provisioning the legacy `dams`-realm editor accounts.
- **Rebrand scope**: internal tokens only; repo names unchanged this pass.
- **Old/new site overlap**: the legacy fcrepo/fin stack and the ported stack will run **side-by-side in production for 6+ months**. This is a planned overlap window, not a short rollback margin — it drives the namespace-naming decision in Phase 7 and the cutover shape in Phase 9.

### Critical discovery — coordinate before building the indexing/ingest pieces
`argonath` already has real, in-progress code that overlaps with this plan's Elasticsearch-indexing and bulk-import needs: `argonath/dagster/lib/assets.py` (`index_for_dams_search` — a working Dagster asset calling `digtk public-dc es upsert`, deliberately mirroring `dams/services/client/models/item/{model,transform}.js`'s ES envelope; `reconcile_collection_items` — bulk collection ingest, looks implemented; `ingest_item` and `index_for_rag` are explicit `NotImplementedError` stubs marked "phase 0.3"/"phase 0.7"). **This looks like a second, currently uncoordinated track building the same replacement this plan needs.** Before starting Phase 3/4 below, get a status check and establish an explicit contract with whoever owns that code — don't re-derive the ES envelope design independently.

---

## Phased plan

### Phase 0 — Stabilize the port branch, become a fully independent standup
Get `argo-dcf` buildable/deployable again before building anything new on top of it. **Revised scope (superseding the initial draft below the line): one service, not a fin-lite multi-process split, and `@ucd-lib/fin-service-utils`/`@ucd-lib/fin-api` get removed now, not incrementally** — decided after finding that the current `dams-base-service` Docker image is shared: it's the exact image fin's own still-live generic services (`dbsync`, `gateway`, `workflow`, `gcs`, `uber`) also run from today (`dams-deployment/kustomize/fin/dbsync/overlays/*/deployment.yaml` etc. all point at `dams-base-service:<tag>`), just started with a different `npm run <service>` command against the same `services/client/models` directory. **Confirmed with the user: the ported app is an independent standup** — it gets its own Dockerfile/image going forward, decoupled from `${FIN_SERVER_IMAGE}` and from fin's own npm-script service registry entirely. The legacy fin services keep running for the 6+ month overlap (Phase 9) from their **currently-deployed, already-version-pinned image tags** (`dams-deployment`'s overlays already pin exact tags like `2.8.4`, not `latest`) — as long as nobody bumps dbsync/gateway/workflow/gcs/uber's pinned tags to a new build from this branch, they're unaffected by anything below. That pinning discipline is the actual safety mechanism during the overlap, not caution in the app code.

**Single-service architecture**: no separate `api` process. `services/client/controllers/api.js`'s job (auto-mount each data model's `api.js` router, merge their swagger into one spec) gets refactored into a function `services/client/controllers/api.js` exports (e.g. `module.exports = async function mount(app) {...}`, reusing the loader at `services/client/controllers/models.js`) that `index.js` calls directly and awaits before `app.listen(...)` — one process, one port, mounted at `/api`. (This supersedes the interim `http-proxy`-to-a-second-process wiring already committed on this branch — that gets removed.)

**`@ucd-lib/fin-service-utils`/`@ucd-lib/fin-api` removal, following `aggie-experts` as the guide** (`aggie-experts/webapp/` — a real prior migration off the same fin stack, in the same npm org):
- **Config**: becomes fully local (`services/client/config.js`, no longer requiring/extending `@ucd-lib/fin-service-utils`'s `config`). Needs a careful audit — grep every `config.*` property currently read anywhere in `services/client` that traces back to fin-service-utils' config (known so far: `models.rootDir`, `api.port`, `jwt.cookieName`, `fcrepo.*` used throughout the ES transform/SEO files and `lib/fcrepo-app-config.js`, `gateway.host`, `google.*` for GCS) — this list is not yet complete, treat completing it as the first concrete task, not an assumption.
- **Logger**: match aggie-experts' own pattern (`aggie-experts/commons/lib/logger.js`) — a thin wrapper around the real, independently-published `@ucd-lib/logger` package (`createLogger`/`logReqMiddleware`), not anything fin-specific. Add `@ucd-lib/logger` as a real dependency; write a small local `services/client/lib/logger.js` wrapper.
- **Keycloak**: replace `@ucd-lib/fin-service-utils`'s `keycloak.setUser`/`.protect()` with a local `services/client/lib/keycloak.js`, following `aggie-experts/webapp/lib/keycloak.js`'s shape (hand-rolled OIDC verification via `fetch` against Keycloak's userinfo/JWKS endpoints, `setUser` middleware + `protect(roles)`) — reconcile with (likely replace) the existing `services/client/lib/auth.js`, which already does its own JWT-cookie parsing but currently delegates the actual token validation to fin-service-utils' `jwt.validate()`. This is also where Phase 5's realm change (`dams` → `internal`) gets wired in, so build it against the new realm from the start rather than pointing it at the old one first.
- **`middleware.httpTiming()` / `controllers.health`**: no direct aggie-experts equivalent found for the exact httpTiming helper — write small local replacements (a basic request-duration log middleware, matching or reusing `@ucd-lib/logger`'s `logReqMiddleware` if it covers this; a plain `GET /health` route returning 200). Not worth over-engineering; this is a handful of lines.
- **`FinDataModel`/`FinEsDataModel` base classes**: every existing model (`models/{item,collection,client-edits,page-search,application}/model.js`) extends one of these. Replace with a local base class mirroring aggie-experts' `webapp/lib/es-model.js` (`EsDataModel` — raw `@elastic/elasticsearch` client calls: `get`, `esSearch`, `ensureIndex`, etc.) for the ES-backed models, and a plain class for `client-edits` (which only used `FinDataModel` for fin's dbsync `is()`/`update()` hooks and an `@ucd-lib/fin-api` fcrepo `.fetch()` — both dead weight once fin/dbsync isn't the thing populating this model; safe to delete outright now rather than port, since Phase 6 replaces this model's write path wholesale anyway). `seo`'s `index.js` has no model/api at all (transform helpers only) — untouched.
- **`@ucd-lib/fin-api`** itself (used directly by `client-edits/model.js`'s `.fetch()` and by `services/client/lib/fcrepo-app-config.js`) gets dropped alongside the above — `fcrepo-app-config.js`'s job (crawl `/application/ucd-lib-client` for app config, refresh on change) either becomes dead code once the `app-config`/CaskFS work (Phase 2/6) replaces it, or gets a thin CaskFS-calling rewrite then; no need to keep it fin-api-compatible in the meantime if it's just going to be replaced.
- Add real npm deps this now needs: `swagger-jsdoc`, `body-parser`, `fs-extra`, `yaml`, `@ucd-lib/logger`, `@elastic/elasticsearch`, `jsonwebtoken`/`jwks-rsa` (for the local keycloak module) — drop the `npm link @ucd-lib/fin-service-utils`/`npm link @ucd-lib/fin-api` build steps entirely.
- Write dams-specific swagger component files (`services/client/lib/swagger/{parameters,schemas,responses,requestBodies}.json`) — start minimal (empty objects), fill in as real routes get documented.

**New, independent Dockerfile/build**: since this is no longer `FROM ${FIN_SERVER_IMAGE}` and no longer needs anything from the `fin` repo, `services/client/Dockerfile` gets rewritten against a plain base image (e.g. `node:22-bookworm`, matching what fin's own base image already used, just without inheriting fin's layers) rather than patched. `.cork-build`'s `dams-base-service` entry updates accordingly. Per the rebrand decision (internal tokens only, this pass), the image name can stay `dams-base-service` for now, but **whoever cuts releases must not reuse a version tag already in use by the legacy `dbsync`/`gateway`/etc. deployments** — flagging as an operational rule, not a code change, since those overlays already pin explicit tags rather than tracking `latest`.

- **Reference material**: no prose migration doc exists for aggie-experts' own fin removal — the closest thing is the git history on their `dc-fin-lite-round1`/`dc-fin-lite-api` branches (merge commits `05e8d3f`, `f7447c0`, and earlier `2d13150`), worth a `git show`/`git log -p` pass for a step-by-step diff before starting this work.
- **Done when**: a local build succeeds off this branch with zero `@ucd-lib/fin-service-utils`/`@ucd-lib/fin-api` references anywhere in the *active* code path (excluding: each model's `transform.js`, kept unrequired as reference material for Phase 3's argonath-envelope coordination; `models/collection-import/`, unmounted/unreferenced dead code whose eventual replacement is Phase 4's argonath ingestion, not this pass), the single-process server starts, and `GET /api` returns a real swagger doc listing the registered models, even before `app-config`/fcrepo-shim work lands. **Verified 2026-09-01**: `node index.js` starts cleanly with no live Postgres/Elasticsearch/Keycloak available (everything connects lazily), `GET /health` returns 200, `GET /api` returns the merged swagger doc for all 6 registered models. Two pre-existing broken relative-require paths left over from the `72d52ac` rename (`index.js`'s `./models/sitemap` → `./controllers/sitemap`, and `controllers/sitemap.js`'s `../../models/{collection,item}` → `../models/{collection,item}`) were found and fixed along the way.

### Phase 1 — Solve dev/test access to a non-prod CaskFS
`argonath-deployment` has zero dev environment. Two tracks, not mutually exclusive:
- **Interim**: dams `local-dev`/`dev` environments call `argonath-prod`'s real `cask` service directly, scoped to the already-named `gold/digital-dev` slot in argonath's `cask-conventions.md`.
- **Real fix** (needed before prod cutover regardless): a genuine `argonath-dev` namespace in `argonath-deployment` — this is that repo's own work, flag it as an external dependency/ticket, not something this plan does directly. **Confirmed 2026-09-02**: `argonath-deployment/.cork-kube-config` defines only a `prod` environment (namespace `argonath-prod`) — no dev/staging entry exists anywhere in that repo, and every `kustomize/*/` service (including `cask`, `anduin-gateway`) has only an `overlays/prod` directory.
- **Done when**: a dams dev pod can call `GET cask.../api/fs/...?metadata=true` and get a real result back.

**Confirmed path prefix**: `/cask/api/fs/...`. CaskFS's Express router mounts the file/metadata controller at code-level `/api/fs` (`caskfs/src/controllers/{index,fs}.js`); `argonath-deployment/kustomize/cask/base/deployment.yaml` sets `CASKFS_WEBAPP_PATH_PREFIX=/cask` on top of that. Both the plan's original `/api/fs` and `cask-conventions.md`'s `/cask/fs` were each half-right.

**Corrected auth/ACL mechanism** (differs from the original assumption above, verified by reading `caskfs`'s and `auth-gateway`'s source and confirmed with a live local test — see below): CaskFS's ACL is **username-keyed, not Keycloak-role-keyed**. `auth-gateway` does map Keycloak roles matching `caskfs-<name>` to a `roles` array it forwards inside the `x-user` header, but CaskFS's own `header-auth` middleware, while it does parse a `roles` field out of that header into `req.user.roles`, never reads it anywhere else (confirmed via a repo-wide grep — `req.user.roles` has exactly one reference, the assignment itself). All real permission resolution happens inside CaskFS's own Postgres-backed ACL system (`src/lib/acl.js`), keyed by **username** — a CaskFS-internal role, unrelated to Keycloak, gets directory permissions via `setDirectoryPermission`, and specific usernames get assigned to that CaskFS-internal role via `setUserRole`. Practically: the real security boundary for cask access is *whoever can reach cask's port with a trusted `x-user` header* (`auth-gateway`, or a `NetworkPolicy` restricting who can reach cask directly) — not a Keycloak role name. auth-gateway's own routing also does not gate the cask proxy route behind login at all (`accessAllowed()` returns true unconditionally for it); auth-gateway forwards to cask regardless of whether the caller is authenticated, it just won't attach an `x-user` header if they aren't.

**Verified 2026-09-02 (local only, not run against real argonath-prod)**: brought up `project-anduin`'s `postgres`+`cask` containers locally (Docker Desktop) with `CASKFS_ACL_ENABLED=true` and `CASKFS_HEADER_AUTH_ENABLED=true` (both are `false`/unset in `project-anduin/compose.yaml`'s checked-in defaults — overridden for this test only), ran `cask init-pg`, then bootstrapped via the `cask acl` CLI (direct-pg mode, temporarily with `CASKFS_ACL_ENABLED=false` for the bootstrap step only, since granting the very first permission is otherwise a chicken-and-egg `AclAccessError: Admin access required`): created role `dev-digital`, user `dams-dev-service`, assigned the role to the user, wrote a test file to `/gold/digital-dev/test.txt`, and granted `dev-digital` `write` on `/gold/digital-dev`. With ACL re-enabled and calling the live HTTP server from a separate container on the same Docker network (`docker run --network project-anduin_default curlimages/curl ...`, mirroring cross-namespace k8s service-DNS calls):
- `GET http://cask:3001/cask/api/fs/gold/digital-dev/test.txt?metadata=true` with `x-user: {"username":"dams-dev-service"}` → **200**, real metadata including the resolved on-disk CAS path (`fullPath`) that Phase 2's IIIF direct-mount needs.
- Same path, actual file bytes (no `?metadata=true`) → **200**, correct content.
- Same user, a path with no grant (`/gold/digital-prod/...`) → **403**.
- No `x-user` header at all (anonymous) → **403**.
- Unknown username with no role assignment → **403**.

Default-deny confirmed; scoping to a subtree works as designed. **Still open, not testable locally**: whether argonath-prod's actual GKE cluster has any `NetworkPolicy` blocking cross-namespace traffic from dams's namespace to `argonath-prod` — Docker Desktop's single-node cluster can't surface that, it can only confirm k8s-style DNS resolution and the application-level ACL behavior, both of which now check out.

**Deliverables from this pass** (files only — no changes executed against real argonath-prod, per explicit instruction):
- `services/client/config.js` — new `config.cask` block (`CASK_URL`/`CASK_PATH_PREFIX`/`CASK_USER`, defaulting to `cask.argonath-prod.svc.cluster.local:3001` / `/cask`).
- `dams-deployment/kustomize/configmap/overlays/dev/configmap.yaml` — same three vars added for the `digital-dev` namespace, `CASK_USER: dams-dev-service`.
- `dams-deployment/cmds/provision-cask-dev-access.sh` — runbook script (not auto-run) with the exact `cask acl` commands to run by hand against the real argonath-prod cask pod (`kubectl -n argonath-prod exec deploy/cask -- cask acl ...`), mirroring the sequence verified locally above.
- `dams-deployment/compose/local-dev/` — a standing (not one-off) docker-compose local dev environment, following `aggie-experts-deployment/compose/local-dev`'s convention (sandboxed images + source-mounted sibling-repo volumes): `postgres`, `cask`, `auth-gateway` (argonath's pieces) plus a `client` service building dams's own `services/client` from source. `docker compose up -d --build && ./bootstrap.sh` gets a fully working local stack; verified from-scratch (`down -v` → `up --build` → `bootstrap.sh`) that the dams server container itself, using its real `config.cask` values, gets 200s from cask on the granted path and 403s outside it. See that directory's `README.md`. Two pre-existing bugs found and fixed getting this to build, both leftover from the `72d52ac` rename (same class as the two Phase 0 already caught): `services/client/Dockerfile`'s `npm install --omit=dev` skipped the `webpack`/`webpack-cli` devDependencies the very next `RUN npm run build` step needs (changed to plain `npm install`); `client/public/lib/models/SeoModel.js` required `../../../../../models/seo/index.js` (5 `../`, resolving outside the service dir entirely) instead of the correct `../../../../models/seo/index.js` (4 `../`).

### Phase 2 — Wire the fcrepo-middleware shim; IIIF direct-mount
**Implemented and verified 2026-09-10** (local-dev only, per below): `services/client/lib/fcrepo-middeware.js` rewritten (was a stub with the three branches unimplemented) and wired into `index.js` (`app.use(require('./lib/fcrepo-middeware.js'))`, before `controllers/static`). New `services/client/lib/cask.js` is the CaskFS HTTP client the middleware uses (`resolvePath()`, `getMetadata()`, `fileUrl()`). New `services/client/tests/lib/{cask,fcrepo-middleware}.test.js` (mocha+supertest, mocking `global.fetch` and the `http-proxy` instance — no real CaskFS needed) — 15 tests, green. This is the testing strategy's item 4, done alongside the implementation rather than deferred.

**Proxy, not redirect**: none of the backend services (IIIF, cask, etc.) are exposed directly to the internet; the dams client/server (`services/client`) is the sole public front door and acts as a reverse proxy to everything behind it — the same shape fin's own gateway has today. Every branch resolves the target internally and **forwards the request server-side via `http-proxy`** (`fcrepo.web(req, res, {target, ignorePath: true, ...})`, path rewritten per-branch), never a `302` sending the browser to a different host.

**ARK resolution — resolved, differently than assumed**: the "open risk" originally flagged here (does `/fcrepo/...`'s path need ARK→cask-path resolution, and is `GET /api/ld` the way to do it) is resolved: `GET /api/ld` is still a 404 stub, but CaskFS's `GET /api/find?subject=<uri>` (backed by its RDF graph, not `/api/ld`) already works for this — `lib/cask.js#resolvePath()` treats the fcrepo-style path (ARK, or ARK+child-binary-path) as an RDF subject URI and looks up the file that carries it, via the file's JSON-LD sidecar `@id` per argonath's `docs/cask-conventions.md` ("Item Identity"). **Two prerequisites this surfaced, verified against the Phase-1 local-dev stack**, both now required reading before trusting this mechanism anywhere else:
1. **CaskFS only indexes RDF subjects that match an explicit allowlist.** `GET /api/find?subject=` silently returns empty for *any* subject unless it matches `CASKFS_FILTER_URIS`/`CASKFS_FILTER_URI_MATCHES` (env-configured on the CaskFS server itself, empty by default — confirmed via CaskFS's own `cask harvest-test <uri>` CLI command). Added `CASKFS_FILTER_URI_MATCHES=^ark:` to `dams-deployment/compose/local-dev/compose.yaml`'s `cask` service. **The real argonath-prod cask deployment needs this same setting** (or ARK subjects resolve as 404 everywhere) — add to the Phase 1 runbook / flag to whoever owns `argonath-deployment/kustomize/cask`.
2. **Confirmed CaskFS bug, patched locally (uncommitted) for verification, not yet a real fix**: `caskfs/src/controllers/find.js`'s `GET`/`POST /api/find` handlers never call `getRequestor(req)` — unlike `controllers/fs.js`'s handlers, which do — so `caskFs.rdf.find()` always runs with no `requestor`, and CaskFS's ACL filtering (`generateAclWithFilter`) then only matches publicly-readable directories regardless of a valid, permissioned `x-user` header. Confirmed via `cask find --subject` (direct-pg, ACL bypassed) returning the test file while the equivalent authenticated HTTP call returned `totalCount: 0`, and via reading `generateAclWithFilter`'s SQL (no `requestor` → no `userId` → ACL clause only matches `user_id IS NULL`). **This blocks ARK resolution for anything under an ACL-scoped path (`gold/digital-dev`, `gold/digital-prod`, per Phase 1) in real argonath-prod, not just locally** — needs a real fix/PR in the `caskfs` repo (`ucd-lib/caskfs`, not this branch) before Phase 2 can be trusted beyond local-dev. The one-line fix (thread `getRequestor(req)` into `options.requestor` before calling `caskFs.rdf.find()`, matching `fs.js`'s pattern) is applied locally in the sibling `caskfs` checkout for this verification pass only.
- **`svc:iiif` branch**: resolves the requested path, calls `GET {CASK}/api/fs/{path}?metadata=true`, reads `metadata.fullPath` (the resolved on-disk CAS hash path), and proxies to `{config.iiif.host}/fcgi-bin/iipsrv.fcgi?IIIF={fullPath}{servicePath}` — the same `urlTemplate` convention the old fin-init data used (`services/init/fcrepo/service/iiif.jsonld.json` in the pre-port `dams-231` snapshot: `http://iiif:80/fcgi-bin/iipsrv.fcgi?IIIF={{fcPath}}{{svcPath}}`), just pointed at CaskFS's resolved hash path instead of a gcs-fuse-mounted ARK path. **Verified as far as locally possible**: resolution + metadata lookup confirmed working end-to-end against the Phase-1 dev cask (a real ARK subject resolves to a real `fullPath`); the actual proxy-to-IIIF hop is unverified — local-dev has no `iiif` service/container yet (fails with `ENOTFOUND iiif`, expected). **Still fully open, not started**: the IIIF container's direct, read-only volume mount of CaskFS's CAS root (this is the "spike this early" item below) — this assumes the mount root is identical between the CaskFS pod and the IIIF pod, so `fullPath` is usable verbatim; unconfirmed. CaskFS's `caskfs-volume` PV is NFS-backed, so a second PVC in the frontend's namespace bound to the same NFS export is plausible — but **spike this early**, cross-namespace RWX PVC sharing isn't guaranteed to just work depending on the CSI driver.
- **`svc:gcs` branch**: implemented — strips the legacy bucket-name path segment (as the original stub comment intended), then proxies to CaskFS's resolved item directory + remaining sub-path via `GET /api/fs/...`. Unverified against a real multi-asset item (no such item exists in local-dev cask yet) — confirm exactly which asset types still need this once IIIF reads CaskFS directly.
- **Plain-stream branch**: implemented — proxies the resolved path straight to `GET /api/fs/{path}`, forwarding Range/If-None-Match headers through untouched (CaskFS supports both; `http-proxy` forwards request headers by default). Verified end-to-end against the Phase-1 dev cask (see below).
- **Verified 2026-09-10 (local-dev only)**: wrote a real JSON-LD sidecar (`@id: "ark:/87287/dphase2test"`) to `/gold/digital-dev/item-test/item.jsonld.json` in the Phase-1 local-dev cask via the `cask` CLI, granted the existing `dams-dev-service` identity read access (already had directory-level write from Phase 1's bootstrap), then confirmed `GET http://localhost:8000/fcrepo/rest/ark:/87287/dphase2test` (dams client/server, `services/client/lib/fcrepo-middeware.js` live-mounted into the `client` container) returns the real document with a `200`, resolved and proxied entirely through CaskFS with no other change — the two prerequisites above (`CASKFS_FILTER_URI_MATCHES`, the `find.js` requestor fix) were both required to get there; before either fix this same request 404'd.
- **Open item, not yet resolved, needed before Phase 3/4** (unchanged from before, still the real remaining risk): the exact RDF-subject convention above (a full fcrepo-style path — ARK, or ARK+child-binary-path — as the subject) is a **working assumption**, not a confirmed contract. It happens to be self-consistent and requires no ingestion-side coordination to *use* (CaskFS's path layout can be anything; only the RDF subject matters), but whether argonath's ingestion actually mints an RDF subject per addressable resource this way (vs. only per-item) is still the open "Critical discovery" coordination item from this doc's Context section — confirm before relying on this for a real, argonath-ingested item.
- **Done when** (unchanged, not yet met — local-dev verification above is real but partial): a known test item's IIIF tiles and JSON-LD metadata both load through `/fcrepo/*` URLs against the Phase-1 dev cask, with the browser only ever talking to the dams client/server (confirm via network trace — no direct IIIF/cask host ever appears), and image bytes confirmed served without buffering through Node (response headers/latency check on the proxy hop). Blocked on: a real argonath-ingested test item (Phase 4), the IIIF CaskFS-mount spike above, and the caskfs `find.js` fix landing for real.

### Phase 3 — Replace the ES-indexing trigger (keep Elasticsearch itself; all pipeline work lives in argonath, not dams)
The entire indexing pipeline is owned and run by argonath — this phase is coordination + verification on the dams side, not new dams-side pipeline code:
- Core/raw item data lands in CaskFS **bronze** via ingestion (Phase 4).
- A Dagster asset calls the `digtk`/`dc` CLI to generate **dc-specific silver derivatives** (the digital-collections-shaped processed output — thumbnails, manifests, whatever the dc silver convention requires).
- A Dagster asset then calls the CLI again to **push those documents into the dcf Elasticsearch instance** (this is the `index_for_dams_search`/`digtk public-dc es upsert` code already in progress in `argonath/dagster/lib/assets.py` — coordinate with that track per the "Critical discovery" above rather than re-deriving the envelope).
- Dams-side work here is narrow: confirm the dcf ES instance is reachable from wherever Dagster runs (`argonath-prod`, cross-namespace — same class of problem as Phase 1), confirm/adopt the ES document envelope contract argonath's CLI already targets, and verify.
- Dual-write verification window: fin's dbsync (if still live against fcrepo) and argonath's CLI-driven push both populate the same alias; diff for drift before trusting the new path, especially for multi-page items (flagged by the argonath code's own comments as unverified). Keep fin/dbsync running dormant as a rollback path until Phase 9's decommission step.
- **Done when**: a full collection's ES documents produced by argonath's bronze→silver→ES pipeline are verified equivalent to (or an intentional improvement on) fin dbsync's current output.

### Phase 4 — Bulk metadata backfill
Two distinct ingestion paths, near-term and steady-state:
- **Near-term / manual**: a direct "add a collection via CLI" capability (`digtk`/`dc` CLI, pushing straight into CaskFS bronze) for ad-hoc/one-off collection loads — this is argonath Dagster/CLI work, **partially built already** (`reconcile_collection_items` calls `digtk ingest batch` for `source_type: "digital-collection"` — the git-metadata-repo case). The actual gap is `ingest_item`, an explicit stub. Get a timeline commitment on that before relying on it for a full backfill.
- **Steady-state / long-term source of record**: once rolling, all collection data comes from **Preservica** (a digital-preservation system) as an extract step landing in CaskFS bronze, then transformed into schema.org for dcf's silver/gold layers. **Open item to resolve, not yet researched**: confirm who owns the Preservica-extract step (argonath/Dagster, presumably, matching the CLI-driven bronze→silver→ES pattern from Phase 3) and whether that extraction pipeline exists yet or is itself new work — this materially changes Phase 4's schedule risk depending on the answer.
- Confirm binary/media backfill (actual TIFFs/JP2s, not just JSON-LD/schema.org metadata) into CaskFS bronze for both paths — not explicit in the current ingest code and is the largest data-volume piece of the whole migration.
- **Done when**: one full legacy collection is present in CaskFS and browsable end-to-end through the Phase 2 shim, matching the live site — via the CLI path first, with the Preservica-extract path validated once that pipeline exists.

### Phase 5 — Auth reconciliation (realm `dams` → `internal`)
- Repoint `services/client/lib/auth.js` / `keycloak.setUser` config at realm `internal`, client matching argonath's `anduin-gateway` config.
- Migrate/provision legacy editor accounts into `internal` with `admin`/`ui-admin` roles. Do this in parallel with Phases 2-4 (independent), but complete before Phase 6's end-to-end testing needs real authenticated sessions.
- Decide explicitly (don't leave implicit): does the dams server keep doing its own OIDC verification and become the "trusted gateway" that presents `x-user` to cask, or does it sit behind `anduin-gateway`? Recommend the former (keep the client's own login/session UX; the server's calls into cask carry a `x-user` header derived from its own verified session) — but confirm.

### Phase 6 — Client-edits rework
Rename `services/client/models/client-edits/` → `services/client/models/app-config/`, and register it in the Phase 0 model registry (`models/index.js`) so it's mounted at `/api/app-config` by the single-process mounting logic from Phase 0 — no longer dependent on fin's separate `services/fin/api` auto-mount service at all. `FinDataModel` inheritance and the dbsync `is()`/`update(json)` hooks are already gone as of Phase 0 (deleted outright, not ported); this phase is purely about building the new write API and Postgres schema on top of that already-plain class.

**New Postgres schema** (`services/init/postgres/004-app-config-rebrand.sql`, additive/idempotent — the existing SQL files run as guarded init scripts against a live DB, not a tracked migration history, so this must be a new file, not an edit to `003-client-edits.sql`):
- Idempotent `ALTER SCHEMA dams_edits RENAME TO dcf_edits` guarded by existence checks.
- Replace the old fan-out `dams_edits.edit` table with two simple keyed tables: `dcf_edits.collection_edit(id, edit jsonb, updated_by, updated_at)`, `dcf_edits.item_edit(id, collection_id, edit jsonb, updated_by, updated_at)` (indexed on `collection_id`), plus a new singleton `dcf_edits.featured_collections(config jsonb, ...)` table — the homepage featured-collections widget currently isn't cached to Postgres at all today, this closes that gap too.
- If production has real live edit data, a one-time backfill script reshaping old JSON-LD rows into the new flat tables is needed before dropping the old table — confirm how much real data exists first.
- Drop the unused `http://digital.ucdavis.edu/schema#ClientEdit`/`#DamsClientEdit` RDF vocabulary entirely — confirmed nowhere downstream reads it.

**New routes** (`services/client/models/app-config/api.js`), plain JSON in/out (no `@context`/`@id`/JSON-LD flattening — `cleanEditForApi()` goes away, reads become direct `SELECT`):
```
GET/PUT  /api/app-config/collection/:id
POST     /api/app-config/collection/:id/featured-image   (binary, streamed to CaskFS)
GET/PUT  /api/app-config/item/:id
GET/PUT  /api/app-config/featured-collections
```
- **Server-side auth enforcement** (currently only a client-side `canEditUi()` hide/no-op — nothing stops a crafted request today): gate every write route with `keycloak.protect(['admin','ui-admin'])`, the same mechanism `fin/dbsync` already uses. Put this behind a single seam (`models/app-config/lib/auth.js`) so it's a one-line change if the auth model later shifts to trusting `anduin-gateway`'s `x-anduin-user` header instead.
- **Featured image upload**: stream the request body straight through to `PUT {CASK}/api/fs/gold/digital-{env}/collection/{slug}/featured-image.jpg`, store the resulting cask path as a pointer field in Postgres. Verify `bodyParser.json()`/`bodyParser.urlencoded()` don't interfere with a binary body on this route (first time this server has needed to), and add an explicit size limit (no `Content-Length` guard exists on this path today).
- **Fix the known item-edit-merge gap**: `models/item/model.js` has no `get()`/`search()` override at all (unlike `collection/model.js`'s existing `_appendClientEdits()`) — item-level edits are captured but never served back. Add the equivalent `_appendAppConfigEdits()` override, reading from the new `dcf_edits.item_edit` table.
- **Client changes**: `FcAppConfigService.js`/`FcAppConfigModel.js` swap their direct-to-fcrepo PUTs for calls to the new API and drop the JSON-LD document construction; `CollectionService.js`'s `/api/client-edits${id}` becomes `/api/app-config/collection${id}`; `app-collection.js`/`app-record.js`/`admin-featured-collections.js` get minor field-name updates (`watercolors.css` → `watercolor`, `exampleOfWork` → `highlightedItems`) — intentionally kept close to current names to minimize churn.
- **Sequencing note**: this write path can't dual-run against fcrepo (no natural shadow-write). Cut it over early in dev (low-risk, easy to hand-verify), but hold the *production* cutover for this specific feature until immediately before the full site cutover in Phase 9.

### Phase 7 — k8s/deployment restructuring (`dams-deployment`)
- **The old and new sites will run side-by-side in production for 6+ months** (see Phase 9) — the legacy stack keeps running under its current `digital-dev`/`digital-prod` (and `dams` local-dev) namespaces for that entire window, so the ported stack **cannot reuse those namespace names**; it needs genuinely new, distinct names from day one (not a rename-in-place), e.g. `dcf-dev`/`dcf-prod` (exact names TBD — propose in the branding pass, Phase 10, but the namespace *scaffolding* itself needs to exist well before then, back in this phase).
- **Drop entirely**: `kustomize/{fcrepo,ocfl-volume,gcs-fuse,nfs-server,rabbitmq,collection-import}`, `kustomize/fin/{gateway,dbsync,uber,workflow,gcs,init,binary-mirror}`.
- **Keep, reconfigure**: `postgres` (role narrows — no more `ocfl_id_map`/containment tables), `elastic-search`+`kibana`, `ucd-lib-client` (the sole public front door — an Ingress/LoadBalancer target — and, per Phase 0, the only process/container for the whole app; no separate api deployment), `iiif` (new CaskFS PVC mount per Phase 2 instead of `gcs-fuse`; stays ClusterIP-only, reached exclusively via `ucd-lib-client`'s `http-proxy` forwarding per Phase 2, never given its own public Ingress — matches its current ClusterIP-only config). Confirm whether `pg-rest` is still needed by anything once client-edits talks to Postgres directly through the new API.
- **Dropped, additionally** (beyond the fcrepo/fin services already listed above): the legacy `dams-base-service`-built `fin/{dbsync,gateway,workflow,gcs,uber}` deployments are not touched by this port at all going forward — per Phase 0, they keep running from their currently-pinned image tags through the overlap, then get deleted here once the overlap ends (Phase 9), same as the rest of the legacy stack.
- **Added**: nothing new locally — cross-namespace calls to `cask.argonath-prod.svc.cluster.local` / `anduin-gateway.argonath-prod.svc.cluster.local`, plus the new IIIF PVC.
- **Done when**: a from-scratch deploy of the renamed dev namespace succeeds with only the kept/added services.

### Phase 8 — Smoke-test / health-check replacement
fin's ActiveMQ-based integration-health-test pattern has nothing to listen to anymore. Recommend both layers:
- **Shallow liveness**: extend the existing `controllers.health.register(app)` (`index.js:50`) to actively verify cask, ES, and Postgres reachability.
- **Deep synthetic**: a scheduled job (k8s `CronJob` in the frontend's own namespace, recommended over an argonath-side Dagster schedule since it's testing *dams's* availability specifically) that round-trips one known real item end-to-end (metadata + one IIIF tile), and post-Phase-6 a test client-edit save.
- Alerting destination is undefined in everything explored — needs an owner decision.

### Phase 9 — Cutover and rollback
The old and new sites run side-by-side in production for **6+ months** — a planned overlap period, not just a safety margin. Both stacks (old `digital-dev`/`digital-prod` namespaces and new `dcf-dev`/`dcf-prod` namespaces per Phase 7) need to be independently addressable/operable for that whole span.
1. Old stack (fcrepo/fin/rabbitmq, in its existing namespaces) stays fully live and authoritative through Phases 0-8; all new-path work happens in the new namespaces' dev/sandbox.
2. **Read-path cutover first**: once Phase 3/4 backfill+indexing is verified equivalent for the full catalog, switch production reads (item/collection pages, IIIF, search) to the new path — likely gradually/collection-by-collection given the multi-month window rather than a single flip — leaving fcrepo running read-only as an immediate rollback target throughout. Keep dbsync running dormant as a dual-write safety net until confidence is high.
3. **Write-path (client-edits) cutover last**, atomically, in a low-traffic window, immediately preceded by a final export/comparison of fcrepo's current `application/ucd-lib-client` state against what's about to become authoritative in Postgres.
4. Keep fcrepo/ocfl-volume/gateway/dbsync/rabbitmq (and their namespaces) fully deployed for the entire 6+ month overlap — do not delete `ocfl-volume` until confident no rollback is needed, since it's the only copy of binary content outside whatever's already migrated.
5. Decommission the old stack/namespaces only after the overlap period ends and cutover is confirmed complete and stable.

### Phase 10 — Rebrand pass (deliberately last, isolated)
Internal-tokens-only rebrand (npm package name, Postgres schema naming already done in Phase 6, custom-element/CSS prefixes, GCS bucket names, user-facing text) as its own PR after Phase 9 is stable — bundling it with the functional migration makes any bug much harder to bisect.

---

## Testing strategy (started now, not deferred)
No test framework exists in `dams` or `fin` today; `caskfs` has a working Mocha convention (`.mocharc.yml` + `tests/` + `tests/helpers/`) — mirror it in `services/client` (`services/client/.mocharc.yml`, add `mocha`/`supertest` to `services/client/package.json`, replace the placeholder `"test"` script).

Highest-value initial coverage, aligned to what this plan actually builds:
1. `tests/models/app-config/api.test.js` — new write API round-trips, upsert-on-conflict, 401/403 for non-admin.
2. `tests/models/app-config/featured-image.test.js` — CaskFS-calling logic specifically (stub/mock the CaskFS HTTP endpoint), asserting correct path construction and clean failure (502, not a silent partial write) on a CaskFS error.
3. `tests/models/item/model.test.js` — the item-edit-merge fix (`_appendAppConfigEdits`), since it's a genuine bug fix and the highest regression risk in this rework.
4. **Done (2026-09-10)**: `tests/lib/cask.test.js`, `tests/lib/fcrepo-middleware.test.js` — assert each branch calls CaskFS/IIIF with the expected translated path, mocking `global.fetch` and the `http-proxy` instance, no real CaskFS instance needed.

## Critical files
- `services/client/lib/fcrepo-middeware.js`, `services/client/lib/cask.js`, `services/client/index.js` — the shim (Phase 2, implemented) and its CaskFS HTTP client, and (Phase 0) where the single-process api-mounting call gets awaited before `app.listen`
- `caskfs/src/controllers/find.js` (sibling repo, `ucd-lib/caskfs`) — the requestor-threading bug Phase 2 found and worked around locally; needs a real fix landed there
- `services/client/controllers/api.js`, `services/client/controllers/models.js` (loader), `services/client/models/index.js` (registry) — the single-process model-mounting layer (Phase 0), pattern from `aggie-experts/webapp/lib/api.js`/`webapp/lib/models.js` but merged into one process, not a standalone `api` deployment
- `services/client/lib/config.js`, `lib/logger.js`, `lib/keycloak.js` (new, Phase 0) — local replacements for `@ucd-lib/fin-service-utils`'s `config`/`logger`/`keycloak`, following `aggie-experts/commons/lib/logger.js` and `webapp/lib/keycloak.js`; replaces the existing `services/client/lib/auth.js`
- `services/client/models/{item,collection,client-edits,page-search,application}/model.js` — Phase 0's `FinDataModel`/`FinEsDataModel` removal, following `aggie-experts/webapp/lib/es-model.js`
- `services/client/Dockerfile`, `.cork-build` — Phase 0's new independent (non-fin-based) build
- `services/client/models/client-edits/{model,api}.js` → renamed `app-config/` — Phase 6
- `services/init/postgres/003-client-edits.sql` (reference only, do not edit) + new `004-app-config-rebrand.sql`
- `services/client/client/public/lib/services/FcAppConfigService.js`, `lib/models/FcAppConfigModel.js`, `lib/services/CollectionService.js` — client-side rewrite
- `services/client/models/item/model.js`, `models/collection/model.js` — item-edit-merge fix
- `dams-deployment/.cork-kube-config`, `kustomize/iiif/base/deployment.yaml` — namespace/IIIF-mount changes
- `argonath/dagster/lib/assets.py`, `argonath/exec/digtk/lib/dams_index.py` — the track to coordinate with, not duplicate

## Verification
- Phase 0: `grep -r '@ucd-lib/fin-service-utils\|@ucd-lib/fin-api' services/client` returns nothing outside `package-lock.json`; local build + single-process container start; `GET /api` returns a swagger doc.
- Phase 2: **partially verified 2026-09-10** — a real (hand-written) test item's JSON-LD metadata loads through `/fcrepo/*` against the Phase-1 dev cask end-to-end (mocha unit suite + a live local-dev round trip). Still open: real item's IIIF tiles load the same way (blocked on the IIIF CaskFS-mount spike + a real argonath-ingested item), and confirm image bytes aren't proxied through Node (latency/headers check).
- Phase 3: ES document diff between old (fin dbsync) and new (argonath) paths for a representative multi-page item and a simple item.
- Phase 6: `mocha` suite green; manual click-through of the edit UI (collection hero image, watercolor, item viewer-type) against a dev instance with a real `internal`-realm admin session, confirming both save and correct read-back (including the previously-broken item-edit case).
- Phase 7: from-scratch `cork-kube deploy` of the renamed dev namespace succeeds.
- Phase 9: staged production cutover per the read-then-write sequencing above, with fcrepo left live as rollback until the overlap window closes.
